# gtech-phish

Static React mockup of the login screen.

## Setup
1. Install dependencies: `npm install`
2. Start dev server: `npm run dev`

## Notes
- Logo asset is served from the existing images/ folder.
